import joblib
from sklearn.ensemble import RandomForestClassifier
import pickle
import os

# Define your data
data = {'key': 'value'}

# Save the dictionary to a .pkl file using pickle
data_file_path = 'C:\Users\PC\Downloads\Sentiment model\data.pkl'
with open(data_file_path, 'wb') as file:
    pickle.dump(data, file)

# Create and train the model (assuming X_train and y_train are defined)
model = RandomForestClassifier()
# Assuming X_train and y_train are your training data
model.fit(X_train, y_train)

# Save the trained model to a .pkl file using joblib
model_file_path = 'C:\Users\PC\Downloads\Sentiment model\sentiment_model.pkl'
joblib.dump(model, model_file_path)
